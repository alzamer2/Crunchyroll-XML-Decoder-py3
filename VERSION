### 1.3 rev.21
* fix issue #25, add make code retries login 5 times and 5sec wait time between each retry to make sure us proxy server dont crash
### 1.3 rev.20
* fix issue #24, add more item for special character
### 1.3 rev.19
* fix issue #24, add more item for special character
### 1.3 rev.18
* new and faster usa_seesion_id proxy, its experimental and it worked for me but use "check USA proxy.bat" to make sure its working
### 1.3 rev.17
* fix usa_seesion_id
### 1.3 rev.16
* update access_token
### 1.3 rev.15
* fix autocatch
### 1.3 rev.14
* add code that ensure HLS and DASH code run in console rather than idle
* some minor fix
### 1.3 rev.13
* fix for when there is no settings file
### 1.3 rev.12
* add new class to generate classy menu which will help in editing and adding new function, you can still use crunchy-xml-decoder-py3_old to load old menu
* add new class for Dash stream, now it can be download faster, will add option later that will let you choice which to use as preferred download option
* add DEBUG mode that dump alot of data, can be helpfull if the code begin to go crazy
* add multi argparse argument that can be used from dos like '-d URL'
* minor fix
### 1.2 rev.11
* fix updater problem where he dont return the right lastest version
### 1.2 rev.10
* merged Added -q argument #6
* after crunchyroll added multi streams option (it seem they plan to make site suported for native apple tv os), the code is updated so it only use AES-128 hls stream (fixing issue #15)
* add dash stream support as alternative method if hls stream download failed (no download speed up yet)
### 1.2 rev.9
* add more method to get usa session (fixing issue #12)
### 1.2 rev.8
* add new character to clean_text function
### 1.2 rev.7
* change HLS downloader to class(no idea why i did not do that befoer)
* trying to fix when connection number is double hls parts
### 1.2 rev.6
* fix when only hardsub only present
### 1.2 rev.5
* remove line for Debugging that was prevent muxing dub anime
### 1.2 rev.4
* add option in settings that let use auto catch dub links
* change config reader function to return dict instead of list to make it easier in future to add settings
* did some clean up for code
### 1.1 rev.3
* fix update problem
* add code so that video with identical names dont overwrite each other
### 1.1 rev.2
* add auto check for code version
* add update option
### 1.0 rev.1
#### First stable version after exporting the code was exported from python 2
* new way for login that dont need cfscrape
* add progressbar to download
* add color for console
* use new way to get video url which include Hardsub video and 240p
* new way to get usa sesson_id
* add new proxy function
* cleaned HLS code and removed extra parts, now its faster
